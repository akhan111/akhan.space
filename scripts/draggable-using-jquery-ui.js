
$(function () {
	$("a").draggable({
		revert: "invalid",
//		helper: "clone",
		opacity: 0.35,
		});
});

$(function () {
	$("img").draggable({
		revert: "invalid",
//		helper: "clone",
		opacity: 0.35,
		});
});


$(function () {
	$(".pinterest-feed-container").draggable({
		revert: "invalid",
//		helper: "clone",
		opacity: 0.35,
		});
});