
$(function () {
	$("a").draggable({
		revert: "invalid",
//		helper: "clone",
		opacity: 0.35,
		refreshPositions: true,
		});
});

$(function () {
	$("img").draggable({
		revert: "invalid",
//		helper: "clone",
		opacity: 0.35,
		refreshPositions: true,
		});
});


$(function () {
	$(".pinterest-feed-container").draggable({
		revert: "invalid",
//		helper: "clone",
		opacity: 0.35,
		refreshPositions: true,
		});
});